% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-track-next.R
\name{geom_track_geoms}
\alias{geom_track_geoms}
\alias{geom_track_point}
\alias{geom_track_line}
\alias{geom_track_col}
\alias{geom_track_tile}
\alias{geom_track_text}
\title{Common chromosome track geom wrappers}
\usage{
geom_track_point(
  mapping = NULL,
  data = NULL,
  track,
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)

geom_track_line(
  mapping = NULL,
  data = NULL,
  track,
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)

geom_track_col(
  mapping = NULL,
  data = NULL,
  track,
  position = "stack",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)

geom_track_tile(
  mapping = NULL,
  data = NULL,
  track,
  position = "identity",
  ...,
  clip = c("auto", "on", "off"),
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)

geom_track_text(
  mapping = NULL,
  data = NULL,
  track,
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = FALSE
)
}
\arguments{
\item{mapping}{Aesthetic mappings containing `chr`, `position` and `value`,
plus aesthetics consumed by `geom`.}

\item{data}{Track data frame.}

\item{track}{One identifier declared by [track_layout()].}

\item{position}{A standard ggplot2 position adjustment.}

\item{...}{Parameters and constant aesthetics passed unchanged to `geom`.}

\item{na.rm}{Passed to the geom layer.}

\item{show.legend}{Should the standard ggplot2 guide include this layer?}

\item{inherit.aes}{Whether to inherit plot aesthetics. The ideogram base plot
has no data mapping, so the default is `FALSE`.}

\item{clip}{Tile clipping mode. `"auto"` clips an overlay tile layer to the
chromosome silhouette and leaves beside tracks unchanged. `"on"` requires
an overlay track; `"off"` preserves unrestricted rectangular tiles.}
}
\value{
An additive chromosome-track component.
}
\description{
These functions are small presets over [geom_chr_track()] and retain
ordinary ggplot2 aesthetic, scale and guide semantics. An overlay
`geom_track_tile()` uses a [ggplot2::GeomTile] subclass only to mask its
standard tile output to the chromosome silhouette. All wrappers require
mappings for `chr`, `position` and `value`.
}
