% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track-spec.R
\name{track}
\alias{track}
\title{Declare one chromosome track}
\usage{
track(
  side = c("right", "left", "overlay"),
  width = 1,
  gap = NULL,
  value_scale = c("per_track", "global", "per_chr"),
  limits = NULL,
  transform = "identity",
  reverse = FALSE
)
}
\arguments{
\item{side}{Track placement: `"right"`, `"left"`, or `"overlay"`.}

\item{width}{Width of the value region in chromosome-body-width units.}

\item{gap}{Clear distance from the chromosome body or preceding track. If
`NULL`, the documented default is `0.2` beside a chromosome and `0` for an
overlay.}

\item{value_scale}{Scope of an automatically derived value range:
`"global"` shares one range across all global tracks, `"per_track"`
shares within this track, and `"per_chr"` resolves each chromosome
separately.}

\item{limits}{Optional finite increasing raw-data limits. Explicit limits
make a track independent of layer-addition order and are required by
components whose standard ggplot2 Stat must run after global projection,
including ribbon, area, boxplot and violin tracks.}

\item{transform}{A transformation name understood by
[scales::as.transform()] or a scales transform object.}

\item{reverse}{Reverse the direction from low to high values.}
}
\value{
An `ideogram_track_spec` object, to be named inside
  [track_layout()].
}
\description{
A track owns a stable transverse region beside (or over) every chromosome.
Its dimensions are normalized chromosome-body-width units, while point,
text and line sizes remain ordinary ggplot2 physical units.
}
